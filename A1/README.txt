The program able to use malloc to allocate space for the image. 
I used fgetc() to load the memory space for the image and fwrite() to output the image.
The program hasn't touch thumbnail image. But I also add the pgm data which include: width, height, and maxval.

Instruction (For executable):
 - Click the ppm unix executable

Instruction (For cmd):
 - Open command line or terminal
 - Go to the project directory
 - type "./pgm" to run the program